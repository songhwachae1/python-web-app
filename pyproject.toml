[project]
name = "python-web-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "songhwa", email = "songhwachae76@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
python-web-app = "python_web_app:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
